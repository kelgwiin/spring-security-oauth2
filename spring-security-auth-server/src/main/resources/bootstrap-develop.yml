spring:
  application:
    name: oauth2-test
  profiles:
    active: develop
  cloud:
    config:
      uri: http://localhost:8191


  main:
    allow-bean-definition-overriding: true


logging:
  level:
    org:
      springframework: DEBUG